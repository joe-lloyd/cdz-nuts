# public/ is BUILD OUTPUT but is deliberately committed: apps/server serves it
# off a bind mount and apps/desktop embeds it with include_dir!, and neither
# runs a JS toolchain. CI rebuilds and fails if it drifts from src/.
